[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "praxis-config"
version = "0.1.0"
description = "PRAXIS configuration"
dependencies = [
    "pydantic",
    "pydantic-settings"
]

[tool.setuptools]
py-modules = ["settings"]
